name: barbora_flutter_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.0.0
  connectivity: ^3.0.3
  cupertino_icons: ^1.0.2
  file_cache: ^0.2.0
  http: ^0.13.1
  internet_connection_checker: ^0.0.1
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
